<div class="page-header">
    <h1>Details of note</h1>
</div>


<table class="table table-bordered">
    <tbody>
        <tr><th>title</th><td><%= note.title %></td></tr>
        <tr><th>content</th><td><%= note.content %></td></tr>
        <tr><th>createdAt</th><td><%= note.createdAt %></td></tr>    
    </tbody>
</table>

<div class="well">
    <%- link_to('<i class="icon-edit icon-white"></i> Edit', path_to.edit_note(note), {class: 'btn btn-primary'}) %>
    <%- link_to('<i class="icon-remove icon-white"></i> Delete', path_to.note(note), {class: 'btn btn-danger', method: 'delete', remote: true, jsonp: '(function (u) { location.href = u; })'}) %>
    or
    <%- link_to('Back to index', path_to.notes) %>
</div>